# ScanResponse

This example composes the advertising and scan-response payloads independently.
Each has a 31-byte legacy limit. The sketch comments explain the byte budget and
why identification fields and descriptive fields use different sides.
