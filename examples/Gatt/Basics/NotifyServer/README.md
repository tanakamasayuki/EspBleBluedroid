# Notify Server

Sends a counter notification once per second while a client is subscribed. Subscription changes arrive through `onSubscriptionChanged()` and `notify()` takes the characteristic handle returned during registration.
